config:
  target: "http://localhost:3000"
  phases:
    - duration: 30
      arrivalRate: 5
      name: Warm up
    - duration: 60
      arrivalRate: 5
      rampTo: 50
      name: Ramp up load
    - duration: 60
      arrivalRate: 50
      rampTo: 150
      name: Ramp up load
    - duration: 60
      arrivalRate: 150
      rampTo: 300
      name: Ramp up load
    - duration: 120
      arrivalRate: 300
      rampTo: 600
      name: Sustained load

scenarios:
  - name: "Retrive reviews"
    flow:
      - get:
          url: "/reviews/?page=&count=&sort=&product_id={{$randomNumber(1,999999)}}"
