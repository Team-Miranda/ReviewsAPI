config:
  target: "http://localhost:3000"
  phases:
    - duration: 15
      arrivalRate: 5
      name: Warm up
    - duration: 15
      arrivalRate: 5
      rampTo: 50
      name: Ramp up load
    - duration: 15
      arrivalRate: 50
      rampTo: 200
      name: Ramp up load
    - duration: 30
      arrivalRate: 200
      rampTo: 1000
      name: Ramp up load

scenarios:
  - name: "Retrive meta"
    flow:
      - get:
          url: "/reviews/meta?product_id={{$randomNumber(1,99999)}}"
